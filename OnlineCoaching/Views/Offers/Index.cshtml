@model IEnumerable<OnlineCoaching.ViewModels.Offer.OfferViewModel>

@{
    ViewBag.Title = "Offer page";
}

<h4>All offers</h4>

<ul class="list-group">
    @foreach (var offer in Model)
    {
        @Html.Partial("_OfferSimpleView", offer)
    }
</ul>
